import { Wallet } from "lucide-react";
import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function PaymentsPage() {
  return (
    <PagePlaceholder
      icon={Wallet}
      title="Payments"
      description="Payment recording and balance tracking land in a later development phase."
    />
  );
}
